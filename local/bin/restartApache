#!/bin/sh

#phpファイルを実行して
#結果をアウトプットする
#ディレクトリ内のphpファイルに向けて実行するので
#引数必要なし

#nkfが必要なので、brewで入れて。
#
#phptohtml
for name in *.php

do
inport=${name}
output="${inport%.php}.html"

if [ "${inport%.php}" = '*' ];then
    exit 0
fi
php $inport | nkf -w > $output
echo $output

done

exit 0
