#!/bin/sh

#rubyのインスコ #好きなバージョンでok
RUBIES=( 1.8.7-p371 1.9.3-p392 2.0.0-p0)
cd $HOME
git clone git://github.com/sstephenson/rbenv.git ~/.rbenv

if [ -x `which rbenv` ];then
    echo 'rbenvをインストールします'
    mkdir -p ~/.rbenv/plugins
    cd ~/.rbenv/plugins
    git clone git://github.com/sstephenson/ruby-build.git
    git clone git://github.com/tpope/rbenv-ctags.git
    git clone git://github.com/jamis/rbenv-gemset.git
    echo 'export PATH="$HOME/.rbenv/bin/:$HOME/.rbenv:$PATH"'
    echo 'eval "$(rbenv init -)"'

    for version in ${RUBIES[@]}
    do
       CONFIGURE_OPTS="--with-arch=i386 --with-readline-dir=/usr/local/Cellar/readline/6.2.4 --with-openssl-dir=/usr/local --enable-shared" rbenv install $version
       # CFLAGS="-arch i386" LDFLAGS="-arch i386" 
    done

    rbenv rehash
fi


