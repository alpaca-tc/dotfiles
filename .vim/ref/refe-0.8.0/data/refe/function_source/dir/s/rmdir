dir.c:554
static VALUE
dir_s_rmdir(obj, dir)
    VALUE obj, dir;
{
    SafeStringValue(dir);
    rb_secure(2);
    if (rmdir(RSTRING(dir)->ptr) < 0)
	rb_sys_fail(RSTRING(dir)->ptr);

    return INT2FIX(0);
}
