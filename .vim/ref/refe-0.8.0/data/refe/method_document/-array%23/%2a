--- self * times

    配列の内容を繰り返した新しい配列を作成し返します。
    値はコピーされないことに注意してくださいtrap::Array。

    例:

        p [1, 2, 3] * 3  #=> [1, 2, 3, 1, 2, 3, 1, 2, 3]

    times が文字列なら、self.join(times) と同じ
    動作をします。

        p [1,2,3] * ","
        # => "1,2,3"

