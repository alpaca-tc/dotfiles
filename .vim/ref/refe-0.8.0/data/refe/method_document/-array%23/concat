--- concat(other)

    配列 other を self の末尾に(破壊的に)連結します。
    self を返します。

    例:
        array = [1, 2]
        a     = [3, 4]
        array.concat a
        p array          # => [1, 2, 3, 4]
        p a              # => [3, 4]       # こちらは変わらない

