--- send(mesg, flags[, to])

    ソケットを介してデータを送ります。flags に関しては 
    send(2) を参照してください。connect していないソケット
    に対しては送り先である to を指定する必要があります。実際に送っ
    たデータの長さを返します。

    to にはソケットアドレス構造体を pack した文字列
    を指定します。

    データの送信に失敗した場合は例外 Errno::EXXX が発生します。

