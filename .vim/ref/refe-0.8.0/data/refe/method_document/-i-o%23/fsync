--- fsync       ruby 1.7 feature

    書き込み用の IO に対して、システムコールfsync(2) [manual page] 
    を実行します。flushを行ったあと(OSレベルで)まだディスクに
    書き込まれていないメモリ上にあるデータをディスクに書き出します。

    成功すれば 0 を返します。失敗した場合は例外 Errno::EXXX が発
    生します。self が書き込み用でなければ例外 IOError が発
    生します。

