--- values_at(index1, index2, ...)

    ruby 1.8 feature

    正規表現中の indexN 番目の括弧にマッチした部分文字列の配列を
    返します。0 番目は $& のようにマッチした文字列全
    体を表します。

        m = /(foo)(bar)(baz)/.match("foobarbaz")
        p m.values_at(0,1,2,3,4)   # same as m.to_a.values_at(...)
        p m.values_at(-1,-2,-3)

        => ["foobarbaz", "foo", "bar", "baz", nil]
           ["baz", "bar", "foo"]

