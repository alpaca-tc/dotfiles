--- ENV.values_at(key_1, ..., key_n)

    ruby 1.8 feature

    引数で指定されたキー(環境変数名)に対応する値の配列を返します。存在
    しないキーに対しては nil が対応します。ENV.indexes と 
    ENV.indices と同じです。

        ENV.update({'FOO','foo', 'BAR','bar'})
        p ENV.values_at(*%w(FOO BAR BAZ))   # => ["foo", "bar", nil]

