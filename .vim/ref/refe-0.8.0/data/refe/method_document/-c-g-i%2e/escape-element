--- CGI.escapeElement(string, *element)
      特定の要素だけをHTMLエスケープする。

        例：
        print CGI::escapeElement('<BR><A HREF="url"></A>', "A", "IMG")
          # => "<BR>&lt;A HREF="url"&gt;&lt;/A&gt"

        print CGI::escapeElement('<BR><A HREF="url"></A>', ["A", "IMG"])
          # => "<BR>&lt;A HREF="url"&gt;&lt;/A&gt"


#=== UNESCAPE ELEMENT

