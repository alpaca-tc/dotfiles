--- Vector#[]=(i,x)
    i番目の要素をxに変更する。

このメソッドは、matrix.rbに定義されていない。
以下に、その定義と使用例を示す。

    require 'matrix'

    class Vector
      def []=(i,x)
        @elements[i]=x
      end
    end

    v=Vector[1,2,3]
    v[2]=-1
    p v #=> Vector[1,2,-1]

