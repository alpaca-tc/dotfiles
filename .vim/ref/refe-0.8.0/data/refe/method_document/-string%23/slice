--- slice(nth[, len])
--- slice(substr)
--- slice(first..last)
--- slice(first...last)
--- slice(regexp[, nth])

    self[] [String/[]] と同じです。

    ruby 1.7 feature: slice(regexp, nth) は、version 1.7 以降で
    使用できます。

