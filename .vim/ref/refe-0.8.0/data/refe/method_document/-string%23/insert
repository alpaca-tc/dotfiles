--- insert(nth, other)  ruby 1.7 feature

    nth 番目の文字の直前に文字列 other を挿入します。
    以下と(戻り値を除いて)同じです。

        self[nth, 0] = other

    self を返します。

        str = "foobaz"
        p str.insert(3, "bar")
        # => "foobarbaz"

