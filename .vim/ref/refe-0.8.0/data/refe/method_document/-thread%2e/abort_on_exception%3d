--- Thread.abort_on_exception = yes_no

    いずれかのスレッドが例外によって終了した時に、インタプリタ全体を中
    断させるかどうかのフラグを設定します。通常あるスレッドで起こった例
    外は、Thread#join [Thread/join] などで検出されない限りそのスレッ
    ドだけをなにも警告を出さずに終了させます。

    yes_no を返します。

