--- Math.acos(x)
--- Math.asin(x)
--- Math.atan(x)

    x の逆三角関数の値をラジアンで返します。

    返される値の範囲はそれぞれ [0, +π]、[-π/2, +π/2]、
    (-π/2, +π/2) です。

    acos(x), asin(x) では x は -1.0 <= x <= 1 の範囲内でな
    ければなりません。(普通、NaN を返します)

    ruby 1.7 feature: acos(), asin() は範囲外の引数に対して、例
    外 Errno::EDOM [Errno::EXXX] が発生します。

