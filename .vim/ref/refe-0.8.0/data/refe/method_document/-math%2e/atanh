--- Math.acosh(x) ruby 1.7 feature
--- Math.asinh(x) ruby 1.7 feature
--- Math.atanh(x) ruby 1.7 feature

    x の逆双曲線関数の値を返します。

        asinh(x) = log(x + sqrt(x * x + 1))
        acosh(x) = log(x + sqrt(x * x - 1)) [x >= 1]
        atanh(x) = log((1+x)/(1-x)) / 2     [-1 < x < 1]

    acosh(x) では x はx >= 1 の範囲内でなければなりません。
    (普通、例外 Errno::EDOM [Errno::EXXX] が発生します)

    atanh(x) では x は -1.0 < x < 1 の範囲内でなければな
    りません。(普通、例外 Errno::EDOM [Errno::EXXX] が発生します)

