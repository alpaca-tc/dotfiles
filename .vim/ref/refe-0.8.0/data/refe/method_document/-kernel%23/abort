--- abort
--- abort(message)      ruby 1.7 feature

    Ruby プログラムを異常終了します。exit との違いは、
    呼び出し時に $! が nil でなければその例外の
    メッセージを標準エラー出力に出力することと、プログラムの終了ステー
    タスが 1 固定であることです。

    ruby 1.7 feature: 引数 message を指定すると
    message に message を設定し
    て標準エラー出力に出力します。

