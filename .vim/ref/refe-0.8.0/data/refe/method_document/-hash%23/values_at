--- values_at(key_1, ... , key_n)

    ruby 1.8 feature

    引数で指定されたキーに対応する値の配列を返します。キーに対応する値
    がなければ default の戻り値が使用されます。
    indexes と indices と同じです。

        h = {1=>"a", 2=>"b", 3=>"c"}
        p h.values_at(1,3,4)               # => ["a", "c", nil]

