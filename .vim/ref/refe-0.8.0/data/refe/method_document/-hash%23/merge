--- merge(other)
--- merge(other) {|key, self_val, other_val| ... }
--- merge!(other)
--- merge!(other) {|key, self_val, other_val| ... }

    ruby 1.8 feature

    Hash#merge は、hash.dup.update と同じです。
    Hash#merge! は、Hash#update [Hash/update] の別名です。

