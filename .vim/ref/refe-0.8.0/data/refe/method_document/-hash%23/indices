--- indexes(key_1, ... , key_n)
--- indices(key_1, ... , key_n)

    引数で指定されたキーを持つ値の配列を返します。

    ruby 1.8 feature:
    このメソッドは version 1.8 では、obsolete です。
    使用すると警告メッセージが表示されます。
    代わりに Hash#values_at [Hash/values_at] を使用します。

