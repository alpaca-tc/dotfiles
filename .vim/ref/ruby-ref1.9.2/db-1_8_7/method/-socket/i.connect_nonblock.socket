kind=defined
names=connect_nonblock
visibility=public

--- connect_nonblock(server_sockaddr) -> Fixnum

ソケットをノンブロッキングモードに設定した後、
[[man:connect(2)]] を呼び出します。

引数、返り値は [[m:Socket#connect]] と同じです。

connect(2)がエラーになった場合、
EINPROGRESS を含め例外 [[c:Errno::EXXX]] が発生します。

@param server_sockaddr ソケットアドレス構造体を pack した文字列[[ref:lib:socket#pack_string]]を指定します。

@raise Errno::EXXX connect(2)がエラーになった場合、 EINPROGRESS を含め例外 [[c:Errno::EXXX]] が発生します。

@return [[m:Socket#connect]] と同じです。


