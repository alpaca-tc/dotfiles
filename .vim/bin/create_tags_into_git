#!/bin/sh

git_root_dir=`git rev-parse --show-toplevel`

if [ -d "${git_root_dir}" ]; then
    echo "directory"
    ctags -R --sort=yes --exclude=log --exclude=.git --exclude=.js -f ${git_root_dir}/.git/tags
else
    echo "not directory"
fi
