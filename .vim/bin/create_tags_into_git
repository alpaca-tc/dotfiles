#!/bin/sh

git_root_dir=`git rev-parse --show-toplevel`

if [ -d "${git_root_dir}" ]; then
    /Applications/MacVim.app/Contents/MacOS/ctags -R --languages=ruby --sort=yes -f ${git_root_dir}/.git/tags
fi
