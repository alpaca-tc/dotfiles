snippet OptionParser
abbr    OptionParser
options word
  require 'optparse'

  OptionParser.new do |parser|
    parser.banner = <<~BANNER
      ${1}
    BANNER

    parser.on${2}
  end

# Instance Methods #{{{
snippet on
abbr    parser.on(*opts, &block)
options word
regexp  parser\.
  on('${1:-short_opt}', '${2:--long_option ARG}', '${3:description}') do |${4}|
    ${5}
  end
#}}}
