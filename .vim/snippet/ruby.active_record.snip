snippet     scope
abbr        scope name, -> {}
options     word
  scope :${1:name}, -> {
    ${2}
  }

snippet     default_scope
abbr        default_scope name, -> {}
options     word
  default_scope :${1:name}, -> {
    ${2}
  }
